{
  "name": "wfp-slide-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bookmarklet-activated visual editor for WFP HTML presentations.",
  "scripts": {
    "test": "playwright test",
    "dev": "http-server -p 8080 -c-1 .",
    "build:editor": "node scripts/build-editor.js",
    "check:editor": "node scripts/build-editor.js --check",
    "build:bookmarklet": "node scripts/build-editor.js --check && node scripts/build-bookmarklet.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1"
  }
}
